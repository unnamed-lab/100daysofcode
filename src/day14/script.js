'use strict';

const dataset =
  'In the distance, the sound of waves crashing against the shore could be heard.';

const capitalize = function (str) {
  return str.toUpperCase();
};

const output = capitalize(dataset);
console.log(output);
//  Output: IN THE DISTANCE, THE SOUND OF WAVES CRASHING AGAINST THE SHORE COULD BE HEARD.
